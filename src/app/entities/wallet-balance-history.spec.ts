import { WalletBalanceHistory } from './wallet-balance-history';

describe('WalletBalanceHistory', () => {
  it('should create an instance', () => {
    expect(new WalletBalanceHistory()).toBeTruthy();
  });
});
