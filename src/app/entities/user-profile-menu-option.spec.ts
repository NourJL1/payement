import { UserProfileMenuOption } from './user-profile-menu-option';

describe('UserProfileMenuOption', () => {
  it('should create an instance', () => {
    expect(new UserProfileMenuOption()).toBeTruthy();
  });
});
