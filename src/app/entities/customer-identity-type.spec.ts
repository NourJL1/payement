import { CustomerIdentityType } from './customer-identity-type';

describe('CustomerIdentityType', () => {
  it('should create an instance', () => {
    expect(new CustomerIdentityType()).toBeTruthy();
  });
});
